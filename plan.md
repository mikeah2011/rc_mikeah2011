# API 通知系统实作计划

本计划面向 API 通知系统的最小可行实现 (MVP)。“MVP”表示实现阶段与功能范围，不是系统名称；下文以“最小可行实现”指代这一范围。

## 1. 需求理解与当前状态

- 需求来源：工作目录内的 `AI Coding 作业.pdf`，共两页。
- 当前目录只有需求文件，没有既有应用代码、依赖清单或数据库结构需要兼容。
- 作业目标：提供内部服务，接收业务系统提交的外部 HTTP(S) 通知，并尽可能可靠地投递。
- 评估重点是系统边界、可靠性、复杂度控制，以及如何判断和修正 AI 建议，而不是功能数量。
- 用户偏好 PHP（Laravel）+ REST API + PostgreSQL；已确认最小可行实现包含失败查询与人工重投。
- 本文件是持久保存的实施计划，不代表已经实现。应用代码尚未开始实现；文档纳入本地 Git 版本管理。

## 2. 推荐方案与系统边界

推荐 Laravel 单体代码库，API 与 Queue Worker 独立进程运行，共用 PostgreSQL。

```text
业务系统
   │ POST /api/notifications
   ▼
Laravel REST API
   │ 同一数据库事务：通知记录 + database queue job
   ▼
PostgreSQL（notifications / notification_attempts / jobs / failed_jobs）
   │
   ▼
Laravel Queue Worker ── HTTP(S) ──► 外部供应商
   │
   └── 保存投递结果、安排重试或保留最终失败
```

选择 Laravel 是因为已有队列、HTTP Client、验证、迁移和测试设施；熟悉的技术栈比为了作业引入新框架更有价值。选择 PostgreSQL 数据库队列，是为了减少基础设施，并让接收记录与入队能够原子提交。

### 最小可行实现包含的能力

- 接收已组装好的 URL、HTTP Method、Headers 和 Body，进行鉴权与输入验证。
- 持久化成功后返回 `202 Accepted` 和通知 ID，不等待外部 API。
- 异步投递、超时控制、失败分类、有限重试、投递结果查询和人工重投。
- 以提交幂等键避免业务方因接收响应丢失而创建重复任务。
- 保留必要的尝试记录，支持定位失败原因。

### 最小可行实现不包含的能力与保证

- 不组装广告、CRM 或库存系统的业务参数，也不判断外部业务是否真正执行成功。
- 不实现动态 OAuth 刷新、供应商 SDK、签名脚本或响应到业务对象的转换；先支持业务方提供的静态请求信息。
- 不承诺严格顺序、恰好一次、分布式事务或无限期保证送达。
- 不做管理页面、复杂租户体系、动态路由、独立消息队列或 Kubernetes 部署。
- 不保证业务系统自身的数据提交与本服务接收原子一致；业务方需要该保证时，应在其自身使用 outbox。

## 3. API 与最小数据模型

### API

| 接口 | 用途 |
|---|---|
| `POST /api/notifications` | 创建通知；要求 `Idempotency-Key`，持久化后返回 ID 与状态 |
| `GET /api/notifications/{id}` | 查询本调用方的状态、尝试次数和脱敏失败原因 |
| `POST /api/notifications/{id}/retry` | 对最终失败的通知发起新一轮投递，使用新的重投幂等键 |

请求包含 `url`、`method`、`headers`、可选的 `body`。默认 `POST`，限制在明确支持的方法内，不允许 `CONNECT`、`TRACE`。第一版将 Body 定义为 UTF-8 字符串并按原样发送，避免 JSON 二次序列化破坏供应商签名；二进制、上传文件和流式请求不纳入最小可行实现。

初始请求上限设为 Body 256 KiB、Headers 合计 16 KiB，可通过配置调整。外部响应不返回业务方，也不完整存储；投递器限制响应读取量，避免大响应占满内存。

### 数据表

- `notifications`：UUID、调用方标识、提交幂等键、规范化请求指纹、URL/Method/Headers/Body、状态、投递轮次、尝试计数、最近 HTTP 状态/错误类别、下次尝试时间、成功/失败时间及时间戳。
- `notification_attempts`：通知 ID、轮次、队列尝试标识、开始/结束时间、HTTP 状态、错误类别与耗时；不保存完整响应内容。进程崩溃可能留下没有结束时间的记录，应解释为结果未知，而非成功。
- Laravel `jobs` / `failed_jobs`：任务调度、预留与失败信息；Job payload 只携带通知 ID 和投递轮次，不复制请求凭据。
- 使用 `(client_id, idempotency_key)` 唯一约束处理并发提交。同键同内容返回原通知；同键不同内容返回 `409 Conflict`。

通知业务状态先采用 `pending`、`delivered`、`failed`。`pending` 包含排队、执行与等待重试，避免再维护一套容易卡死的 processing 状态；具体执行过程通过尝试记录表达。

## 4. 可靠性与失败处理

### 接收与入队必须原子化

- API 在一个 PostgreSQL 事务中插入通知和 Laravel database queue job，提交后才返回 `202`。
- 队列与通知使用同一个数据库连接，并确保入队发生在事务内；此路径不使用 `afterCommit()` 或开启队列 `after_commit`，避免通知提交后、入队前崩溃造成漏投。
- 接收失败返回明确错误；业务方可用同一幂等键重试。
- 使用真实 PostgreSQL 的集成测试确认所选 Laravel 版本的事务入队行为，不凭框架默认配置推断。

### 投递语义

- 采用允许重复的至少一次处理模型，同时设置有限自动重试预算；不宣称每条通知一定最终送达。
- 典型不确定场景：供应商已执行请求，但响应丢失，或本地在保存成功状态前崩溃。再次投递可能产生重复副作用。
- 提交幂等键只能防止重复创建通知，不能保证供应商只执行一次。供应商支持幂等时，业务方应提供跨自动重试、人工重投都不变的业务幂等标识。
- Worker 成功保存结果后再结束 Job；已成功或轮次过期的任务直接结束，不再次调用外部 API。

### 默认失败策略

| 结果 | 行为 |
|---|---|
| HTTP `2xx` | 视为 HTTP 层投递成功，不解析业务成功码 |
| 连接失败、网络超时、HTTP `408` / `429` / `5xx` | 自动重试 |
| 其他 `4xx` | 直接最终失败；`409` 等供应商特殊语义留待后续适配 |
| `3xx` | 不自动跟随重定向，记录最终失败 |
| 无效目标、安全策略拒绝或确定性配置错误 | 明确失败，不无意义地重试 |

- 默认每轮最多 8 次队列执行尝试，包含第一次；进程崩溃也可能消耗队列尝试次数，因此不保证实际发出 8 次 HTTP 请求。
- 重试基础间隔为 10、30、120、300、900、1800、3600 秒，并加入小幅随机抖动。
- `429` / `503` 的合法 `Retry-After` 可覆盖较短的退避值，解析秒数或 HTTP 日期并设置可配置上限；非法值使用默认退避并记录原因。
- 默认连接超时 3 秒、单次 HTTP 总超时 15 秒、Job timeout 30 秒、数据库队列 `retry_after` 90 秒，保持 `HTTP timeout < Job timeout < retry_after`。
- 不叠加 HTTP Client 内部自动重试；重试只由队列这一层控制，避免次数和耗时倍增。
- 进程崩溃后，依赖队列 reservation 超时重新领取任务；不依赖内存计时器。
- 确定性失败或重试耗尽时，保存通知最终失败状态。覆盖异常失败、超时和最大尝试次数耗尽的生命周期路径。
- 若数据库故障阻止最终状态更新，保留队列证据并提供轻量 Artisan 对账命令，识别队列已终止但通知仍为 pending 的异常，不盲目重投。

### 人工重投

- 仅允许从 `failed` 开启新轮次，不允许重投 `pending` 或 `delivered`。
- 行锁保护状态判断，在同一事务中增加轮次、重置本轮预算并入队；历史尝试不删除。
- 重投幂等键与新轮次一同持久化，重复请求返回同一轮次，防止响应丢失后误开新轮。
- Worker 更新状态时检查轮次，避免旧任务覆盖新一轮结果。
- 重投不修改原始请求；配置或业务参数需修正时，提交新的通知。

## 5. 最小安全与运维约束

- 仅作为内部服务部署，使用可配置的调用方 API Token；查询和重投按调用方隔离，Token 不写入仓库。
- 外部目标使用配置式域名/端口白名单，禁止 URL 内嵌用户名密码，默认仅允许 HTTPS；HTTP 仅对明确允许的目标开放。
- 任意 URL 转发存在 SSRF 风险：拒绝回环、私网、链路本地与云元数据地址，覆盖 IPv4/IPv6；不能只校验域名字符串或预先解析一次 DNS。
- 连接时必须受约束：使用可验证的地址固定方案或强制出口网络策略，避免校验与实际连接之间的 DNS 重绑定；保持原始 Host/SNI 与 TLS 证书校验，不跟随重定向。
- 过滤 `Host`、`Content-Length`、逐跳 Headers 等不应由调用方覆盖的字段。
- Headers 与 Body 使用 Laravel 加密存储，环境配置保护 `APP_KEY`；日志和查询结果不暴露 Authorization、URL 查询参数或完整请求/响应。密钥轮换和集中凭据管理作为后续能力。
- 提供健康检查、Worker 运行方式、结构化脱敏日志和状态查询；记录积压量、最终失败量和投递耗时的简单查询方式，不先建设监控平台。
- 配置敏感请求数据的保留期与显式清理命令；不得清理仍待投递的通知。清理后幂等键的保留窗口必须在 README 中说明。

## 6. 实作步骤与交付物

| 步骤 | 实作内容 | 完成标准 |
|---|---|---|
| 1. 建立项目骨架 | 初始化兼容环境的 Laravel 稳定版本，锁定依赖；配置 PostgreSQL、database queue、环境变量示例和最简本地启动方式 | API 与独立 Worker 可启动，无 Redis 依赖 |
| 2. 完成接收闭环 | migrations、模型、鉴权、输入/目标验证、提交幂等、事务入队与状态查询 | 仅在持久化成功后返回 202；并发同键不重复建单；回滚不留下孤立记录或任务 |
| 3. 完成投递与失败处理 | HTTP 投递器、Laravel Job、结果记录、退避、超时、失败归档和异常对账命令 | 成功、临时失败、永久失败和崩溃恢复路径行为明确 |
| 4. 完成人工重投与关键测试 | 重投事务、轮次隔离、重投幂等；使用 Laravel/PHPUnit 或项目默认测试工具 | 重复重投不额外建轮次，旧任务不覆盖新状态，敏感数据不外泄 |
| 5. 完成交付说明 | README、AI 使用说明、运行示例和故障演示脚本/操作步骤 | 读者能复现接收、失败重试、最终失败和人工重投，并理解限制 |

建议按 Laravel 常规结构组织：`routes/api.php`、Request 验证类、Controller、Models、`Jobs/DeliverNotification.php`、一个投递服务、目标验证组件、migrations 和 tests。仅对复杂逻辑拆出组件，不为单一实现预建多层抽象。

关键验证场景：

- 同键同内容/不同内容、并发提交、事务回滚和接收响应丢失后的重复提交。
- 外部 `2xx`、`400`、`429`、`500`、连接超时、长期不可用以及最终重试耗尽。
- 使用真实 PostgreSQL 和独立 Worker 验证任务领取、Worker 被终止后的重新投递；只用 queue fake 不能证明可靠性。
- 模拟“外部成功、本地未记录”的窗口，确认重复风险被正确保留和说明，而不是错误宣称恰好一次。
- 并发重投、重投幂等、过期轮次、最终失败状态同步异常与对账恢复。
- SSRF/重定向、调用方隔离、请求与响应大小限制、日志脱敏。

## 7. 工程取舍与演进

### 第一版不采纳的设计

- Kafka/RabbitMQ/Redis：当前没有流量证据需要额外基础设施，数据库队列已经提供持久化调度。以后换外部队列时，必须重新处理业务记录与消息发布的双写问题，例如 transactional outbox。
- 自研轮询调度器与完整租约系统：Laravel database queue 已有任务预留、重试和失败机制；先复用，不重复实现。
- 微服务拆分、管理后台和供应商插件平台：不是当前可靠投递的必要条件，会分散实现与解释的重点。
- 恰好一次承诺：无法仅靠本服务消除外部 HTTP 副作用重复，需要供应商业务幂等配合。
- 完整熔断、租户配额与调度优先级：先用有限重试和人工处理管理复杂度，有实际容量或隔离需求再增加。

### 后续演进触发点

- 队列延迟或数据库争用升高：先看查询与索引、增加 Worker 和清理历史任务，再评估独立队列与 outbox。
- 单一供应商故障拖累其他通知：按目标拆队列、限制并发、增加目标级限流与熔断。
- 凭据或 API 格式频繁变化：引入受控供应商适配、集中 Secret 管理与凭据刷新。
- 运维需求增长：增加指标/告警、失败处理界面、审计与明确的服务目标。

## 8. 最终提交内容与 AI 使用记录

- 仓库名称使用 `rc_{your_nickname}`，当前目录名 `rc_mikeah2011` 符合该形式；本地仓库用于版本管理，远程仓库发布尚未执行。
- README 说明问题理解、架构、接口示例、可靠性边界、失败策略、部署方式和演进依据。
- `AI_USAGE.md` 记录 AI 实际提供的帮助、实际提出但未采纳的建议，以及用户自己做出的决策和原因。
- 已确认的真实决策：AI 起初建议 Python/FastAPI；用户提出偏好 PHP/Laravel。计划据此采用 Laravel，并解释复用其内置能力的价值。
- 后续仅记录真实发生的建议与选择，不把尚未讨论的设计虚构成用户拒绝过的 AI 建议。
- 不确定且不阻碍最小可行实现的条件：真实供应商差异、流量规模、部署环境与长期数据保留要求。先采用上述明确默认值，不对未经测量的吞吐量作承诺。
